// WorkspaceView.swift
import SwiftUI

/// Main editing surface: schema tree on the left, query editor + results on the right.
/// Stub for Phase 2/3.
struct WorkspaceView: View {
    var body: some View {
        VStack {
            Text("Workspace")
                .font(.title)
            Text("Schema tree + query editor land here in Phase 2.")
                .foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
